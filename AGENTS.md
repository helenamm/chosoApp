# AGENTS.md

## Cursor Cloud specific instructions

### Repository State (as of initial setup)

This is a **greenfield repository** for **chosoApp** — a smart household management app. The repo currently contains only `README.md` and `.gitignore` (standard Node.js template). No application code, dependencies, configuration, tests, or build system exist yet.

### Environment

- **Node.js** v22 and npm v10 are available in the environment.
- The `.gitignore` suggests a Node.js/frontend stack (references Next.js, Nuxt, Vite, SvelteKit, Firebase, DynamoDB, Serverless).
- No package manager lockfile exists yet — when one is added, use the matching package manager (`package-lock.json` → npm, `yarn.lock` → yarn, `pnpm-lock.yaml` → pnpm).

### Services

No services are defined yet. When application code is added, update this section with startup/run instructions.

### Lint / Test / Build / Run

No lint, test, build, or run commands are configured yet. When a `package.json` or equivalent is added, update this section accordingly.
